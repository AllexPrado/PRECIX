# Copy this file to .env and fill the values. .env should NOT be committed.
PRECIX_PG_HOST=localhost
PRECIX_PG_PORT=5432
PRECIX_PG_DB=precix
PRECIX_PG_USER=precix_user
PRECIX_PG_PASS=changeme
# Alternative password var if you already use it elsewhere
# POSTGRES_PASSWORD=

# FastAPI/UVicorn
UVICORN_HOST=0.0.0.0
UVICORN_PORT=8000

# CORS / Frontend URLs (optional)
PRECIX_CORS_ORIGINS=http://localhost:5173,http://localhost:8080
# Backend database configuration (no secrets committed)
PRECIX_PG_HOST=localhost
PRECIX_PG_PORT=5432
PRECIX_PG_DB=precix
# Set your DB user and password locally (do not commit real values)
PRECIX_PG_USER=precix_user
PRECIX_PG_PASS=precix2025

# Optional: older compatibility
# POSTGRES_PASSWORD=
